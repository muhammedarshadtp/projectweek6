<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-4">
        <h1 class="text-center text-primary">User Management</h1>
        <div class="row justify-content-between mt-3">
            <div class="col">
                <a href="/adduser" class="btn btn-primary">Add New User</a>
            </div>
        </div>
        <form action="/admin-userSearch" method="post" class="mt-3">
            <div class="input-group">
                <input type="text" name="search" id="search" class="form-control" placeholder="Enter the name of User to be Searched">
                <div class="input-group-append">
                    <button class="btn btn-primary" type="submit">Search</button>
                </div>
            </div>
        </form>
        <table class="table table-bordered mt-4">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>E-Mail</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                <% if(users.length){ for(i=0;i<users.length;i++){ %>
                <tr>
                    <td><%= users[i].name %></td>
                    <td><%= users[i].email %></td>
                    <td>
                        <a href="" class="btn btn-primary">Update</a>
                    </td>
                    <td>
                        <button class="btn btn-danger" type="submit"><a href="/delete?email=<%=users[i].email%>">Delete</a></button>
                    </td>
                </tr>
          <%  }}  %> 
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>